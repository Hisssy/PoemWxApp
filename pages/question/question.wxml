<view id="main">
    <view class="counter">
        <image class="flower" src="../../assets/answer/flower.png"></image>
        <view class="flower-hikari"></view>
    </view>
      <question-section wx:if="{{questShow}}" questType="{{questionType}}" th="{{th}}"></question-section>
    <button class="btn-next" bindtap="questShift">下一题</button>
</view>